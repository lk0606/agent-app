---
description: 每次代码变更须在改动处补必要性注释（含文件头执行顺序、分支旁短注释与示例）
alwaysApply: true
---

# 必要性注释

每次修改或新增代码时，必须在**本次改动的文件**里补必要注释。详细约定见 `docs/current-status.md`「代码注释」小节。

**硬性要求：不能只在文件头 / 函数头写完就交差。** 非显而易见的分支、解析形态、失败边界，必须在**对应代码行旁**写短注释；涉及输入输出时附**一行示例**。

## 写什么

- 业务规则、非显而易见的分支判断
- **文件头说明**：本文件在 Agent 链路中的位置（尤其 server.ts、planner-agent、memory、tools）
- **常规执行顺序**（跨模块）：独立类 / Tool / Runtime 注册表文件头必须写
- **本文件执行链路**（方法标号）：文件头一行索引；在对应方法/步骤上标 `[1]` `[2]`…
- 模块/文件职责（尤其是新 API client、Agent 循环、数据还原逻辑）
- 与另一概念的区别（如 `plannerTrace` vs `toolCalls`、live SSE vs 服务端历史）
- 环境/配置陷阱（如 TokenHub base URL 与旧混元 Key 不通用）
- 为通过 lint 或性能做的非直观写法（注明原因）

## 写在哪里（位置要求）

| 位置 | 何时必须写 |
|------|------------|
| 文件头 / 类头 | 新模块、入口、Tool、Runtime 注册表；**须同时含「常规执行顺序」+「本文件执行链路」**（见下） |
| 函数 / 方法 JSDoc | 非平凡函数；写清「输入→输出」或职责边界 |
| **分支 / case / 形态切换旁** | `if` 多形态解析、安全拦截、与另一路径互斥的失败处理 —— **必须贴在该分支上，不能只靠函数头概括** |
| 配置 / 魔法值旁 | 默认值、环境开关、单价、超时等「为什么是这个数」 |

解析、路由、沙箱路径、工具入参等多形态逻辑：每个形态旁写 **合法例 + 非法例（可选）**，说明为何不落入另一分支。

## 文件头两段顺序（硬性）

独立类 / Tool / Registry 等单文件模块，文件头块注释**必须分两段**写清顺序，不要混成一段：

| 段名 | 写什么 | 读者问题 |
|------|--------|----------|
| **常规执行顺序** | **跨模块**谁先谁后：调用方（Planner / TaskRunner / HTTP）如何进来、出去后谁接 | 这个文件挂在整条 Agent 链的哪一环？ |
| **本文件执行链路** | **文件内**步骤：在对应方法/关键步骤上标 `[1]` `[2]`…；文件头只留一行索引 | 打开本文件后按编号跳读 |

适用：`confirmation-registry.ts`、`running-task-registry.ts`、`task-metrics.ts`、`tools/*` 等。

要求：

1. 「常规执行顺序」写 **跨模块** happy path；旁路各 1～2 行
2. 「本文件执行链路」**不要**在文件头展开成长树；改为：
   - 文件头一行索引：`[1] execute → [2] parseInput → …`
   - **在对应方法 JSDoc / 步骤旁标 `[1]` `[2]`…**（入口方法、私有方法、execute 内关键步骤均可）
3. **不要**只写职责散文；**不要**把跨模块顺序误当成文件内链路（或反过来）

```typescript
// ✅ Tool：文件头索引 + 方法上标号
/**
 * …
 * 本文件执行链路：见方法上的 [1]…[5]
 *   [1] execute → [2] parseInput → [3] resolveSafePath → [4] 字节校验 → [5] 写盘并返回
 */

/** [1] 入口：… */
async execute() { … }

/** [2] 解析输入：… */
private parseInput() { … }

/** [3] 安全路径：… */
private resolveSafePath() { … }

// execute 内：
// [4] 字节校验
// [5] mkdir + writeFile
```

## 风格（后端小白友好）

- 用「这一层负责什么 → 数据从哪来到哪去」写，而不是复述代码字面意思
- 入口文件（server.ts、create-agent-runtime.ts、TaskRunner）优先写文件头块注释
- 表/字段可点名（如 `planner_steps`、`sessions.summary`），方便对照 SQL 迁移
- 分支注释宜短：1–3 行；示例用反引号包输入/输出

## 不写什么

- 一眼能看懂的 import、赋值、标准 CRUD
- 重复函数名/变量名含义的废话注释
- 把【E 节】学习要点整段贴进源码
- **只写函数头、分支里一片空白**（尤其 parse / resolve / 安全校验）
- **文件头只有一句话职责、没有常规执行顺序 / 本文件执行链路**（Tool / Registry 类）

## 示例

```typescript
// ❌ 多余
// 设置 sessionId
setSessionId(id);

// ❌ 不足：只在函数头写了「支持 JSON 和两行」，分支旁没有任何说明
private parseInput(raw: string) {
  if (trimmed.startsWith("{")) { /* 无注释 */ }
  // ...
}

// ✅ 有价值
// 历史只还原 answer 步骤；规划/工具细节在调试面板的 plannerTrace
steps: answer ? [{ id: "answer", kind: "answer", ... }] : [],

// ✅ 有价值
// 默认走 TokenHub；旧 api.hunyuan.cloud.tencent.com 的 Key 不能复用
hunyuanBaseUrl: process.env.HUNYUAN_BASE_URL ?? "https://tokenhub.tencentmaas.com/v1",

// ✅ 有价值（函数头：输入→输出示例）
/**
 * 从 LLM 自然语言抽相对路径；空输入表示沙箱根。
 * 示例：`""` → `"."`；`"/etc"` → `"/etc"`（保留形态，交给 resolveSafePath 拦截）
 */
private extractRelativePath(rawInput: string): string { ... }

// ✅ 有价值（分支旁：形态 + 合法/非法例 + 为何不回退另一路径）
// 形态 A：JSON（模型常把 function arguments 整段塞进来）
// 合法例：{"path":"hitl-demo.txt","content":"hello-hitl"}
// 非法例：{"path":"a.txt"}（缺 content）→ 下方 BAD_REQUEST，勿再当两行文本解析
if (trimmed.startsWith("{")) {
  try {
    // ...
  } catch {
    // 以 { 开头但 JSON.parse 失败 → 明确报 JSON 错，避免误走两行分支
    throw new AppError("BAD_REQUEST", "WriteFileTool received invalid JSON input.");
  }
}
```

## 交付检查

改完代码后自检：

1. 本次 diff 里新增的函数/模块/配置，读者能否不翻文档就理解「为什么这样写」？
2. **多分支 / 多形态解析**：每个关键分支旁是否有短注释（必要时带示例）？还是只有函数头一句概括？
3. **独立类 / Tool**：文件头有跨模块「常规执行顺序」；「本文件执行链路」是否在**对应方法上标了 [1][2]…**（文件头仅索引）？
4. 若第 2～3 条做不到，补注释后再交付。
