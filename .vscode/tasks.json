{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "api-contract: build",
      "type": "shell",
      "command": "pnpm --filter @agent-app/api-contract build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
  ]
}
