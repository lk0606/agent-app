NEXT_PUBLIC_AGENT_API_BASE_URL=http://localhost:3000
# E.13：仅当后端 apps/api/.env 设置了 API_AUTH_TOKEN 时才需要填；留空且后端也留空则不受影响
# 注意：NEXT_PUBLIC_* 会打进浏览器 bundle，不是安全的密钥保管方式，仅用于本地单人学习场景
NEXT_PUBLIC_AGENT_API_TOKEN=
